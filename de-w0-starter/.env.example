# Copy this file to .env and fill values. Never commit .env
DB_URL=postgresql://user:pass@localhost:5432/app # pragma: allowlist secret (example only)
API_KEY=changeme # pragma: allowlist secret (example only)
